id: go-algorithms-004
title: "Merge Sort"
difficulty: 3
tags: [algorithms, sorting, divide-and-conquer, recursion]
description: |
  Implement merge sort. Read N integers and print them sorted ascending, space-separated.

  Example:
    Input:  6\n5 2 8 1 9 3
    Output: 1 2 3 5 8 9
starter_code: |
  package main

  import (
      "bufio"
      "fmt"
      "os"
      "strings"
  )

  func mergeSort(arr []int) []int {
      if len(arr) <= 1 {
          return arr
      }
      // divide and conquer
      return arr
  }

  func merge(left, right []int) []int {
      // merge two sorted slices
      return nil
  }

  func main() {
      reader := bufio.NewReader(os.Stdin)
      var n int
      fmt.Fscan(reader, &n)
      arr := make([]int, n)
      for i := range arr {
          fmt.Fscan(reader, &arr[i])
      }
      sorted := mergeSort(arr)
      parts := make([]string, len(sorted))
      for i, v := range sorted {
          parts[i] = fmt.Sprintf("%d", v)
      }
      fmt.Println(strings.Join(parts, " "))
  }
test_cases:
  - input: "6\n5 2 8 1 9 3\n"
    expected_output: "1 2 3 5 8 9\n"
    time_limit_ms: 5000
    memory_limit_kb: 65536
  - input: "1\n42\n"
    expected_output: "42\n"
    time_limit_ms: 5000
    memory_limit_kb: 65536
  - input: "5\n5 4 3 2 1\n"
    expected_output: "1 2 3 4 5\n"
    time_limit_ms: 5000
    memory_limit_kb: 65536
hints:
  - "Split slice in half: mid := len(arr)/2"
  - "Recursively sort left and right halves"
  - "Merge two sorted slices: use two pointers i, j"
  - "Append remaining elements after one side is exhausted"
solution: |
  package main

  import (
      "bufio"
      "fmt"
      "os"
      "strings"
  )

  func merge(left, right []int) []int {
      result := make([]int, 0, len(left)+len(right))
      i, j := 0, 0
      for i < len(left) && j < len(right) {
          if left[i] <= right[j] {
              result = append(result, left[i])
              i++
          } else {
              result = append(result, right[j])
              j++
          }
      }
      result = append(result, left[i:]...)
      result = append(result, right[j:]...)
      return result
  }

  func mergeSort(arr []int) []int {
      if len(arr) <= 1 {
          return arr
      }
      mid := len(arr) / 2
      left := mergeSort(arr[:mid])
      right := mergeSort(arr[mid:])
      return merge(left, right)
  }

  func main() {
      reader := bufio.NewReader(os.Stdin)
      var n int
      fmt.Fscan(reader, &n)
      arr := make([]int, n)
      for i := range arr {
          fmt.Fscan(reader, &arr[i])
      }
      sorted := mergeSort(arr)
      parts := make([]string, len(sorted))
      for i, v := range sorted {
          parts[i] = fmt.Sprintf("%d", v)
      }
      fmt.Println(strings.Join(parts, " "))
  }
metadata:
  version: "1.0.0"
  content_version: 1
  module: programming
  language: go
  category: algorithms
